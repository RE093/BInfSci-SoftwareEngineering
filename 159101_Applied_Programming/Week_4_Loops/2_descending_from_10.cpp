#include <iostream>
using namespace std;

/**
    Write a program that displays the numbers 10 to 1 (descending order), using the most appropriate loop.
 */

int main() {
    for(int i = 10; i > 0; i--) {
        cout << i << endl;
    }
}